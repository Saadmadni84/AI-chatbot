server:
  port: 8080

spring:
  application:
    name: chatbot-backend
  main:
    banner-mode: off

logging:
  level:
    root: INFO
    com.chatbot.backend: DEBUG

openai:
  api:
    key: ollama # Not used but kept for structure
    url: http://localhost:11434/api/chat
  model: llama3.2
  temperature: 0.7
